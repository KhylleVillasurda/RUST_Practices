# Generated by Cargo
# Will contain compiled code and executables
/target/

# Remove Cargo.lock from ignore if it's an application (binary)
# Keep it commented if it's a library to allow version flexibility
# Cargo.lock

# These are build output directories for tools like `rust-analyzer`
# or specific IDE integrations that might build the project elsewhere.
**/target/

# Files that might be created by your editor or OS
*.swp
*~
.DS_Store
.thumbs.db

# Optional: Ignore the `rls` (Rust Language Server) directory
.rls/

# Optional: Ignore the `rust-analyzer` working files
.rust-analyzer/